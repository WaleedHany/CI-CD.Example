name: Continous integration and develpment

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code 🔔
        uses: actions/checkout@v4

      - name: Setup .net 6
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'

      - name: Restore depemdencies 🛠
        run: dotnet Restore AnimalCountingDatabase.Api\AnimalCountingDatabase.Api.csproj

      - name: Build application 🚀
        run: dotnet build AnimalCountingDatabase.Api\AnimalCountingDatabase.Api.csproj -c Release 